import { Meteor } from 'meteor/meteor';
import "../imports/api/ball"
import "../imports/api/user"

 /* No es necesario el startup si esta vacio y se importan las demas
	* funcionalidades */
Meteor.startup(() => {
  // code to run on server at startup
});
